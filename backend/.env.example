# MySQL-Verbindung
DATABASE_URL="mysql://user:password@localhost:3306/padel"

# Lange, zufällige Zeichenkette! z. B. mit: openssl rand -base64 48
JWT_SECRET="CHANGE_ME_zufaelliges_langes_secret"

# Frontend-URL(s) für CORS, kommagetrennt
CORS_ORIGIN="http://localhost:5173"

PORT=4000
