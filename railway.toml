[build]
buildCommand = "npm install --prefix backend --include=dev && npm install --prefix frontend --include=dev && npm run build --prefix frontend && npm run prisma:generate --prefix backend"

[deploy]
startCommand = "cd backend && npm start"
healthcheckPath = "/api/health"
